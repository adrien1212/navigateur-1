package navigateur;

import static org.junit.Assert.assertFalse;
import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

public class SetupCheckerTest {
	
	@Test
	public void setupChecker() {
		assertTrue(true);
		assertFalse(false);
	}
}
